# Optional anonymous hearts API. Leave empty until the Worker and database exist.
# Local preview: http://127.0.0.1:8787 (use localhost only for development).
# Production: HTTPS origin of the deployed Worker, without /v1/hearts.
NEXT_PUBLIC_HEARTS_API_URL=
